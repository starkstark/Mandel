Interactive Mandelbrot calculation

Use the jar as follows:
$ java -jar Mandel.jar

Left mouseclicks pick the center of the image. Zooming is done via Mousewheel. Images can be saved as as png-files. 